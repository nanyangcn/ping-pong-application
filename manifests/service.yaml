apiVersion: v1
kind: Service
metadata:
  name: ping-pong-application-svc
spec:
  type: ClusterIP
  selector:
    app: ping-pong-application 
  ports:
    - protocol: TCP
      port: 2345 
      targetPort: 3002 